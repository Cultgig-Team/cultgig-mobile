# Onboarding assets

Drop exported images from Figma here.

## Needed now
- `welcome-bg.jpg` — the hero photo on the Welcome screen (Figma node "Artist 119").
  Export it from Figma desktop: select the image layer inside the "Artist 119" frame,
  right-click -> Export -> PNG or JPG @2x, save it here with this exact filename.

The screen falls back to a solid brand-color background if this file is missing,
so the app still runs without it — just won't match the design until added.
